package pages;

/**
 * Служебный класс-описание.
 * Здесь перечислены элементы главной страницы (https://qa-scooter.praktikum-services.ru/),
 * которые участвуют в тестовых сценариях, и локаторы для каждого из них.
 * Сами локаторы используются в Page Object классах пакета {@link pages}.
 * Класс не участвует в выполнении тестов и создан для наглядности (шаг «Изучить тестовые сценарии»).
 */

    // Логотип Яндекса в шапке (ссылка на yandex.ru, открывается в новой вкладке)
    // By.cssSelector(".Header_LogoYandex__3TSOI")

    // Логотип Самоката в шапке (ссылка на главную страницу трена жёра)
    // By.cssSelector(".Header_LogoScooter__3lsAR")

    // Кнопка «Заказать» в шапке (верхняя точка входа в заказ)
    // By.xpath("(//button[text()='Заказать'])[1]")

    // Кнопка «Заказать» под блоком «Как это работает» (нижняя точка входа в заказ)
    // By.xpath("(//button[text()='Заказать'])[2]")

    // Кнопка «Статус заказа»
    // By.xpath("//button[text()='Статус заказа']")

    // Поле ввода номера заказа (появляется после клика на «Статус заказа»)
    // By.cssSelector("input[placeholder='Введите номер заказа']")

    // Кнопка «Go!» — перейти к статусу заказа
    // By.xpath("//button[text()='Go!']")

    // Баннер cookie и кнопка согласия «да все привыкли»
    // By.xpath("//button[text()='да все привыкли']")

    // ==== Блок FAQ «Вопросы о важном» ===

    // Контейнер аккордеона с вопросами
    // By.cssSelector(".accordion")

    // Заголовок (кнопка-стрелочка) отдельного вопроса аккордеона
    // By.cssSelector(".accordion__button")

    // Текст ответа отдельного вопроса аккордеона (скрыт, пока вопрос не раскрыт)
    // By.cssSelector(".accordion__panel")

    // === Форма заказа. Шаг 1 «Для кого самокат» ===

    // Поле «Имя»
    // By.xpath("//input[@placeholder='* Имя']")

    // Поле «Фамилия»
    // By.xpath("//input[@placeholder='* Фамилия']")

    // Поле «Адрес: куда привезти заказ»
    // By.xpath("//input[@placeholder='* Адрес: куда привезти заказ']")

    // Поле «Станция метро» (поиск по вводимому тексту)
    // By.xpath("//input[@placeholder='* Станция метро']")

    // Пункт выпадающего списка станций метро (кликабельный вариант по названию станции)
    // By.xpath("//div[@class='select-search__select']//li//button[text()='НАЗВАНИЕ_СТАНЦИИ']")

    // Поле «Телефон: на него позвонит курьер»
    // By.xpath("//input[@placeholder='* Телефон: на него позвонит курьер']")

    // Кнопка «Далее» (переход к шагу 2)
    // By.xpath("//button[text()='Далее']")

    // ==== Форма заказа. Шаг 2 «Про аренду» ===

    // Поле «Когда привезти самокат» (открывает календарь react-datepicker)
    // By.xpath("//input[@placeholder='* Когда привезти самокат']")

    // Ячейка календаря с конкретным днём (без класса disabled/outside-month)
    // By.cssSelector(".react-datepicker__day:not(.react-datepicker__day--disabled):not(.react-datepicker__day--outside-month)")

    // Выпадающий список «Срок аренды»
    // By.cssSelector(".Dropdown-control")

    // Пункт выпадающего списка «Срок аренды»
    // By.xpath("//div[@class='Dropdown-option' and text()='ТЕКСТ_ПУНКТА']")

    // Чекбокс цвета «чёрный жемчуг»
    // By.id("black")

    // Чекбокс цвета «серая безысходность»
    // By.id("grey")

    // Поле «Комментарий для курьера»
    // By.xpath("//input[@placeholder='Комментарий для курьера']")

    // Кнопка «Назад»
    // By.xpath("//button[text()='Назад']")

    // Кнопка «Заказать» (отправка формы на шаге 2)
    // By.xpath("//div[contains(@class,'Order_Buttons')]/button[text()='Заказать']")

    // === Модальные окна заказа ===

    // Заголовок модалки подтверждения «Хотите оформить заказ?»
    // By.xpath("//div[text()='Хотите оформить заказ?']")

    // Кнопка «Да» в модалке подтверждения
    // By.xpath("//div[@class='Order_Modal__YZ-d3']//button[text()='Да']")

    // Кнопка «Нет» в модалке подтверждения
    // By.xpath("//div[@class='Order_Modal__YZ-d3']//button[text()='Нет']")

    // Заголовок модалки успешного оформления заказа «Заказ оформлен»
    // By.xpath("//div[text()='Заказ оформлен']")

    // Кнопка «Посмотреть статус» / крестик закрытия модалки успеха
    // By.cssSelector(".Order_Modal__YZ-d3 button")

    // === Страница статуса заказа (/track?t=...) ===

    // Заголовок «Такого заказа нет» (для несуществующего номера)
    // By.xpath("//*[text()='Такого заказа нет']")

